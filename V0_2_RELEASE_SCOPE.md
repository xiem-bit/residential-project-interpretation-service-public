# v0.2 RC1 发行范围

状态：`candidate_local_verified / business_cold_start_pending / not_tagged`

## 发行目标

让没有私有工程历史、没有特定 Agent 运行时的使用者，能够从一套已授权原始材料开始，复刻住宅项目竞争力生产流程，独立形成竞争问题、购买任务、统一语义核、三至四条超级竞争力及其 UE 业务落地。

## 公开层次

| 层次 | 公开职责 |
| --- | --- |
| 生产核心 | 方法、任务合同、产物1／2、统一语义核、SC成立、产物启用、UE投影、变更重投影 |
| 外部协作合同 | 甲方资料、公开信息、机构数据、专家输入的任务与回传边界 |
| 平台适配器 | WorkBuddy、Codex、通用Agent、演示文稿与网页的能力路由说明 |
| 私有保留 | 真实项目、客户资料、受限资产、黄金答案、隐藏答案、凭据和运行现场 |

## 必须随包

- 根入口、Agent规则、发行状态、权威映射和机器可读生产路径；
- 九份核心业务规范；
- 项目任务、三张附表、事实缺口、产物1／2、语义核、SC、产物启用、第二／三章、产物4／5和变更模板；
- 项目身份、产物1／2、语义核、SC、UE桥接和跨产物一致性校验；
- 平台中立生产编排 Skill；
- 一套从原始输入开始的完整虚构教程；
- 一套语义变化与定向重投影教程；
- 隐藏答案任务模板、盲审量表和观察回执；
- 清洁、泄密、依赖、许可证和干净克隆测试。

## 发布门禁

1. 当前候选的结构和负例测试通过；
2. 干净克隆在标准 Python 环境中通过；
3. 隐藏答案材料没有泄露预期语义；
4. 第二位使用者或外部 Agent 仅凭固定候选和原始输入完成生产；
5. 真人盲审达到量表阈值；
6. 最终回执达到 `production_path_replication_pass`；
7. 才允许创建并推送 `v0.2.0-rc.1` 标签。

## 明确非门禁

- PPTX 页面美观；
- Office 像素级渲染；
- 任一特定演示文稿 Skill；
- 云发布或真实客户采纳。

这些能力可以独立验收，但不能证明住宅战略发现能力。
