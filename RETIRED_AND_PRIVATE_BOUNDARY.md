# 现行、私有替代与历史退役边界

状态：`p0_frozen / current_release_boundary`

本文件只解决一个问题：哪些内容代表现行生产能力，哪些需要公开安全替代，哪些属于历史退役或运行现场，不能被误算为公开包能力缺口。

## 一、现行能力

以下内容必须进入当前公开发行路径：

- 产物 1 默认、产物 2—5 按需启用的产品体系；
- 项目身份、输入职责、产物 1／2、统一语义核、三至四条 SC、跨产物投影与变更回写；
- 增量检索、证据角色、回执接受／拒收和双包接口；
- 允许／禁止推导关系、高风险正反例、状态诚实和甲方表达门槛；
- 产物 1、2、3、4、5 的公开安全黄金参考；
- 机器否决型 Harness、生产回执和发行验证。

现行能力以 `CAPABILITY_PARITY_CONTRACT.md`和`CAPABILITY_PARITY_MANIFEST.json`为发行冻结入口。

## 二、私有内容的公开安全替代

| 私有内容 | 公开替代方式 | 必须保留 | 必须移除 |
| --- | --- | --- | --- |
| 真实甲方资料与项目成果 | 匿名或虚构的结构等价项目 | 资料结构、冲突难度、论证链、章节与完成度 | 项目名、客户名、真实数字、权益和原文件 |
| 产物 1／2黄金报告 | 公开安全甲方报告 | 研究镜头、决策结构、断言表达和来源邻接 | 城市、竞品、客群与项目专属结论 |
| 产物 3黄金终稿 | 公开安全完整PPT与页面语义组合 | 页面职责、视觉结构、删改逻辑和首稿门槛 | 原项目文案、图片、客户、页脚和品牌信息 |
| 产物 4正式样例 | 公开安全单树样例 | 上层价值、深层生产、稳定编号桥 | 原项目事实和客户路线 |
| 产物 5黄金实现 | 项目中立可运行源码 | 状态机、动效、AI推荐、二维码、手机页和QA | 项目事实、图片、问答、路线、域名和发布配置 |
| 真人修订记录 | 去身份学习反馈 | 原稿问题、修改、可迁移原因和边界 | 人名、客户名、真实运行现场和内部消息 |

若无法在不损失能力的前提下形成公开安全替代，该能力保持`release_blocked`，不能以删除逻辑的方式宣称完成。

## 三、历史退役，不进入当前安装入口

1. `v0.1.0-rc.1`只作为冻结语义核后的合同与载体演示标签，不升级为完整生产能力；
2. 原产物 6 已并入产物 1，不保留并行产品入口；
3. 固定五个 Skill、十五对象、E3／E4、重复 ACK、逐题补洞和复杂内部证明链不进入公开生产路径；
4. 特定下游平台的GUI协调、memory、邮箱、检查点编排、模型版本和企业安装过程不进入平台中立核心；
5. 特定项目名称、竞品、客群、SC、发布地址和验收状态不构成通用规则；
6. 固定十二文件、未启用产物的空占位、产物3合同全局必交和隐藏答案盲审作为普通生产前置，属于当前候选待移除的入口漂移；
7. 历史密封题、私有答案和教学期错题不随公开包安装；发行回归只使用公开 fixture 和完整公开安全项目；
8. 已删除 Schema、旧 GUI 回执、旧坐标、旧路径和与当前渠道路线冲突的模板不进入构建链。

历史内容通过既有 Git 标签或私有研发历史定点追溯。当前安装只读取现行入口和明确标记的`production_reference`、`learning_feedback`及公开回归 fixture。

## 四、运行现场，不构成可继承能力

- 用户账号、Cookie、token、浏览器 profile、扫码与登录态；
- Computer Use、浏览器或其他插件在某台电脑上的安装与授权；
- 当次微信、小红书、网页、地图、发布服务器或云环境健康状态；
- 本机绝对路径、工作树、临时缓存、运行日志和进程；
- 某次测试、演示、发布或客户验收状态。

公开包负责说明前置条件、检测缺口和返回真实状态，不迁移或假装继承这些现场能力。

## 五、判定规则

- 历史路线退出当前入口，不等于其事故经验全部丢弃；已经升格为现行关系义务的经验进入`CAPABILITY_PARITY_CONTRACT.md`和公开正反例；
- 私有材料不能公开，不等于对应生产能力可以省略；必须形成结构等价替代；
- 公开包文件更多，不等于能力更完整；以同构清单中的行为、成果和验证结果为准；
- 终端安装完成，不等于渠道就绪、业务成果通过或客户采用。

