# v0.2.0-rc.8｜请求一致性、授权与空白初始化修复

原合同要求混合充分性及数量门槛时，上游回包必须保持相同标准，不能通过事后降低原请求门槛完成采用。本版与公开信息工具包v0.8.0-rc.6联合修复这条路径。

- 确定性承接原请求，绑定原请求完整指纹、验收条件与实际充分性输入；住宅校验继续拒绝口径不一致。
- 联动检查从既存住宅请求出发，实际调用上游封装入口，再检验住宅消费；增加模式、数量、质量、多样性和请求身份漂移反例。
- 原范围内迭代、明确停止、范围外扩展分别处理。后续授权由住宅Owner在有效用户授权内形成独立决定，不倒改已执行请求。
- 新建运行只产生草稿、待确认身份、未冻结输入及未运行检查。启用产物不代表生产准入，完成标准保持。
- 保留旧项目和检索结果。旧包进入新采用流程前按原请求重核并重包；安装更新不自动启动研究。

本版沿用原有公开素材范围，不包含用户项目、同事任务记录、账号或运行现场。验证只证明离线合同、包内入口及指定反例，不代表真实渠道、异机试用或业务验收。固定标签及GitHub Release标识实际发行状态，旧版保留。
